/** This component is no longer used — families are no longer separate.
 *  The connection chain handles visibility within a single family. */
export function CopyToFamiliesButton() {
  return null;
}
